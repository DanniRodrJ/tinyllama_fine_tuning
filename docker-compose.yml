# docker-compose.yml

version: '3.8'

services:
  llm-assistant:
    # 1. Construye la imagen usando el Dockerfile en este directorio
    build:
      context: .
      dockerfile: Dockerfile
    
    # 2. Mapea el código local al contenedor
    volumes:
      - .:/app
    
    # 3. Define el comando de ejecución
    # Ejecuta el pipeline principal de Python
    # command: python src/main.py
    
    # 4. Asigna más memoria/CPU si es necesario (opcional)
    # resources:
    #   limits:
    #     memory: 8G 
    #     cpus: '4'

    # 5. Mantiene el contenedor activo para inspección (opcional, si quieres debugging)
    entrypoint: ["/bin/bash"] 
    tty: true